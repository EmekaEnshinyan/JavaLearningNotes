Why is the super modifier needed within nested methods

Is there any reason to assign attributes for methods to call rather than setting parameters or vice versa? how exactly is the process different?
